#!/bin/bash
FILE="/usr/local/google/home/zhangkun/DriveFileStream/My Drive/orgwork/work.org"

# Disable create-lockfiles because it doesn't work well with Google Drive
emacs "$FILE" \
      --eval="(setq create-lockfiles nil org-agenda-files \"$FILE\" frame-title-format \"orgwork\" )"
