#!/bin/bash
if [[ "$#" -lt 1 ]]; then
    echo "Usage: dn-alarm <time>"
    desktop-helper-client.py notify 'The alarm will show like this.'
    exit 1;
fi
at "$1" <<< "desktop-helper-client.py notify 'An alarm set by dn-alarm has expired.'"
