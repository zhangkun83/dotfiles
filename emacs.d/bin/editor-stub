#!/bin/bash
# To be run in the shell-mode as the default editor

echo '######################################################'
echo 'To open the file in current emacs session, press C-c o'
echo 'Press Enter to continue after finishing editing.'
echo '######################################################'
mkdir -p $(dirname "$1")
touch "$1"
echo -n $1
read
